{
  "name": "new-folder",
  "version": "1.0.0",
  "description": "form validation",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browersify main.js -o bundle.js"
  },
  "author": "Jalissa Williams",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "uniq": "^1.0.1"
  }
}